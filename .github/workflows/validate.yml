name: Validate Archive

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Validate archive files
        run: ./scripts/validate.sh
      - name: Run validator fixtures
        run: ruby -Itest test/validator_test.rb
